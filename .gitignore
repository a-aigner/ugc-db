.env
node_modules/
.DS_Store
*.log
.superpowers/

# Higgsfield CLI credentials written into ./generator/auth/ by the
# container so they survive rebuilds — never commit them.
generator/auth/*
!generator/auth/.gitkeep

